# InterviewSync — AI Career Coaching CRM

> **Hebrew RTL, AI-powered career coaching platform** for mentors working with students and career seekers.  
> Final project for a DevOps graduation course — monolith-first, cloud-ready architecture.

[![CI](https://github.com/EyalLin2/InterviewSync/actions/workflows/ci.yml/badge.svg)](https://github.com/EyalLin2/InterviewSync/actions/workflows/ci.yml)
[![Deploy](https://github.com/EyalLin2/InterviewSync/actions/workflows/deploy.yml/badge.svg)](https://github.com/EyalLin2/InterviewSync/actions/workflows/deploy.yml)

---

## Architecture

```
Browser  →  Frontend Flask BFF :5001  →  Backend FastAPI :8000  →  PostgreSQL :5432
              (Jinja2 + RTL UI)           (REST, JWT, async)        (SQLAlchemy ORM)
```

All services run in Docker. The frontend is a thin BFF (Backend-for-Frontend) that holds the JWT session and proxies all data to the stateless FastAPI backend.

---

## Quick Start (Local)

```bash
git clone https://github.com/EyalLin2/InterviewSync
cd InterviewSync
docker compose up --build
```

| URL | Purpose |
|-----|---------|
| http://localhost:5001 | App (login: `admin / admin123` or `student1 / student123`) |
| http://localhost:8000/health | Backend health check |
| http://localhost:8000/docs | Swagger UI (auto-generated by FastAPI) |

---

## Features

### 👨‍💼 Admin — Private CRM (`/admin/private`)
| Feature | Description |
|---------|-------------|
| **Focus Panel** | Morning workflow: inactive students, overdue tasks, pending submissions, unconfirmed meetings |
| **Risk Scores** | AI-computed green/yellow/red risk dot per student (based on inactivity, overdue tasks, velocity) |
| **Student Roster** | Progress bars, last activity, bulk status actions (active / paused / completed) |
| **Student File** | Profile · AI coaching strategy · CV editor · mentor notes · task assignment with due dates |
| **AI Task Suggestions** | Groq LLM generates 5 personalized tasks per student — assign directly or save to bank |
| **Submission Inbox** | All completed tasks in one view with inline feedback fields |
| **Task Bank** | Global reusable task library with CRUD, categories, file attachments |
| **Meeting Scheduler** | Hebrew calendar, WhatsApp confirmation flow, outcome notes saved after meeting |
| **AI Chat** | Admin chats with full student context (tasks, velocity, category breakdown, meetings) |

### 💼 Admin — Business (`/admin/business`)
| Feature | Description |
|---------|-------------|
| **Workshops** | One-time / recurring, status lifecycle (planned → completed) |
| **Inquiries** | Lead tracking, source, notes, assign to workshops |
| **Activity Log** | Professional activity timeline |
| **Calendar** | Unified view — student meetings + business workshops with color legend |
| **Billing** | Monthly invoicing per student, service catalog, paid/unpaid tracking |

### 🎓 Student Portal
| Feature | Description |
|---------|-------------|
| **Task Dashboard** | Active tasks with urgency badges (overdue / due-soon), completed tasks with feedback |
| **Task Q&A** | Ask questions on any task — mentor replies inline; new reply badge notification |
| **AI Self-Chat** | Student asks AI about their own progress, next steps, weak areas |
| **Progress Analytics** | Category skill breakdown bars, velocity stats, days in process |
| **Schedule** | Upcoming meetings with confirm flow; past meetings show outcome summary |
| **Settings** | Edit profile (name, email, phone, goals) + change password |

---

## Tech Stack

| Layer | Technology |
|-------|-----------|
| Frontend BFF | Python 3.11, Flask 3.0, Jinja2 RTL, Heebo font |
| Backend API | Python 3.11, **FastAPI 0.111**, **Uvicorn**, PyJWT |
| Database | PostgreSQL 15 (SQLAlchemy 2.0, standalone `DeclarativeBase`) |
| Auth | JWT (7-day tokens, `HTTPBearer`) |
| AI | Groq `llama-3.3-70b-versatile` (optional — features disabled if key missing) |
| Notifications | Twilio WhatsApp (optional) |
| Scheduler | APScheduler — daily inactivity reminders |
| UI | Bootstrap 5.3.3 RTL, Hebrew RTL layout |
| Containers | Docker, Docker Compose |
| Helm | Charts in `frontend/charts/` + `backend/charts/` (Deployment, Service, Ingress) |
| CI/CD | GitHub Actions: lint → test → Docker Hub push |
| Tests | pytest + FastAPI TestClient (PostgreSQL integration tests in CI) |

---

## Project Structure

```
InterviewSync/
├── backend/                  # FastAPI REST API — stateless business logic
│   ├── app.py                # 60+ endpoints (auth, students, tasks, meetings, AI, billing)
│   ├── models.py             # 11 SQLAlchemy models (User, Task, Meeting, Comment, ...)
│   ├── tests/                # pytest integration tests (real PostgreSQL)
│   ├── Dockerfile
│   ├── requirements.txt
│   └── charts/               # Helm chart — Deployment + Service
│
├── frontend/                 # Flask BFF — serves Hebrew RTL UI, holds JWT session
│   ├── app.py                # All page routes + backend API proxy
│   ├── templates/            # 13 Jinja2 templates (Hebrew RTL)
│   │   ├── base.html, login.html, onboarding.html
│   │   ├── admin_hub.html            ← dual-mode landing
│   │   ├── admin.html                ← student CRM + focus panel + risk dots
│   │   ├── student_file.html         ← student detail: profile / AI / tasks / notes
│   │   ├── student_settings.html     ← student self-service
│   │   ├── admin_submissions.html    ← submission inbox with inline feedback
│   │   ├── admin_business.html       ← workshops / inquiries / activities
│   │   ├── admin_schedule.html       ← unified calendar + outcome recording
│   │   └── index.html, student_schedule.html, meeting_confirm.html
│   ├── Dockerfile
│   ├── requirements.txt
│   └── charts/               # Helm chart — Deployment + Service + Ingress
│
├── .github/workflows/
│   ├── ci.yml                # Lint → Test → Build (on push + PR)
│   └── deploy.yml            # Docker Hub push (staging: main / prod: v*.*.*)
│
├── docker-compose.yml        # Local dev: postgres + backend + frontend
├── Makefile                  # make test / make lint / make coverage / make build
└── setup.cfg                 # flake8 config
```

---

## Environment Variables

### Backend
| Variable | Required | Default | Description |
|----------|----------|---------|-------------|
| `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD` | ✅ | — | PostgreSQL connection |
| `SECRET_KEY` | ✅ | dev key | JWT signing key |
| `GROQ_API_KEY` | ⬜ | — | Groq LLM (AI features disabled if missing) |
| `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_WHATSAPP_FROM` | ⬜ | — | WhatsApp notifications |
| `FRONTEND_URL` | ⬜ | `http://localhost:5001` | Used in meeting confirmation links |
| `INACTIVITY_DAYS` | ⬜ | `7` | Days before inactivity reminder fires |

### Frontend
| Variable | Required | Description |
|----------|----------|-------------|
| `BACKEND_URL` | ✅ | Backend URL (e.g., `http://backend:8000`) |
| `SECRET_KEY` | ✅ | Flask session signing key |

---

## CI/CD Pipeline

### CI (`ci.yml`) — runs on every push + PR
```
Lint (flake8) → Test (pytest + real PostgreSQL) → Docker Build
```

### Deploy (`deploy.yml`) — Docker Hub
| Trigger | Job | Tags pushed |
|---------|-----|-------------|
| Push to `main` | Staging | `:latest` + `:<sha>` |
| Git tag `v*.*.*` | Production | `:<version>` + `:stable` |

**Setup required** (GitHub → Settings → Secrets):
```
DOCKERHUB_USERNAME   your Docker Hub username
DOCKERHUB_TOKEN      Docker Hub access token
SLACK_WEBHOOK_URL    (optional) Slack notifications
```

**Branch protection** (GitHub → Settings → Branches → Add rule for `main`):
- ✅ Require status checks to pass before merging
- ✅ Require pull request before merging

**Feature branch convention:** `feature/RND-123-description`

---

## DevOps Roadmap

| Phase | What | Status |
|-------|------|--------|
| 1 | Docker Compose local + GitHub CI → Docker Hub | ✅ Done |
| 2 | Minikube local, `helm install` both charts | ⏳ After app stable |
| 3 | ArgoCD on Minikube, sync from Docker Hub, Prometheus/Grafana | ⏳ |
| 4 | AWS infra Terraform repo (VPC + EKS + ECR modules) | ⏳ |
| 5 | Switch to ECR, full production on EKS | ⏳ |

---

## Completed Feature Phases

| Phase | Feature | Status |
|-------|---------|--------|
| 1 | Feedback loop — task submission + mentor feedback + WhatsApp | ✅ |
| 2 | Student self-service — edit profile + change password | ✅ |
| 3 | Task due dates — urgency badges + overdue focus panel | ✅ |
| 4 | Submission inbox + bulk student status actions | ✅ |
| — | FastAPI migration — Flask backend → FastAPI + Uvicorn | ✅ |
| 5 | AI risk panel — green/yellow/red risk scores on dashboard | ✅ |
| 6 | Student portal — Q&A on tasks, AI chat, progress analytics, meeting outcomes | ✅ |

## Upcoming

| Phase | Feature |
|-------|---------|
| 7 | Smart notifications — overdue WhatsApp alerts + milestone celebrations |
| 8 | Test coverage 60%+ |
